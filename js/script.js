const menuBtn=document.querySelector('[data-menu]');const menu=document.querySelector('[data-chapter-menu]');if(menuBtn){menuBtn.addEventListener('click',()=>menu.classList.toggle('open'))}
let audioCtx,windGain,birdTimer,playing=false;const btn=document.querySelector('[data-sound]');function startSound(){audioCtx=new(window.AudioContext||window.webkitAudioContext)();const bufferSize=2*audioCtx.sampleRate;const noise=audioCtx.createBuffer(1,bufferSize,audioCtx.sampleRate);const out=noise.getChannelData(0);for(let i=0;i<bufferSize;i++)out[i]=(Math.random()*2-1)*.12;const source=audioCtx.createBufferSource();source.buffer=noise;source.loop=true;const filter=audioCtx.createBiquadFilter();filter.type='lowpass';filter.frequency.value=520;windGain=audioCtx.createGain();windGain.gain.value=.025;source.connect(filter).connect(windGain).connect(audioCtx.destination);source.start();birdTimer=setInterval(()=>{const osc=audioCtx.createOscillator();const gain=audioCtx.createGain();osc.type='sine';osc.frequency.setValueAtTime(1500+Math.random()*900,audioCtx.currentTime);osc.frequency.exponentialRampToValueAtTime(2300+Math.random()*1200,audioCtx.currentTime+.08);gain.gain.setValueAtTime(0,audioCtx.currentTime);gain.gain.linearRampToValueAtTime(.035,audioCtx.currentTime+.02);gain.gain.exponentialRampToValueAtTime(.001,audioCtx.currentTime+.18);osc.connect(gain).connect(audioCtx.destination);osc.start();osc.stop(audioCtx.currentTime+.2)},3500+Math.random()*2000)}
if(btn){btn.addEventListener('click',()=>{if(!playing){startSound();playing=true;btn.textContent='Sound on';}else{audioCtx.close();clearInterval(birdTimer);playing=false;btn.textContent='Sound off';}})}
